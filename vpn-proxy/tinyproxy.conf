User nobody
Group nobody
Port 8118
Timeout 600
LogLevel Info
PidFile "/tmp/tinyproxy.pid"

# LAN-only. This proxy's egress is a paid VPN tunnel — it must never be reachable
# from outside the LAN, so nothing else strips its allowlist without noticing.
Allow 127.0.0.1
Allow 192.168.1.0/24
