[pytest]
# Lives at the repo root (not in docker/) so the deploy folder holds only what the
# server needs — pytest finds it by searching upward from wherever it's invoked, and
# its cache lands here (rootdir) instead of inside the deploy copy.
pythonpath = docker
testpaths = tests
asyncio_mode = auto
