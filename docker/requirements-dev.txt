# Test-only dependencies. Not installed in the production image (Dockerfile only
# installs requirements.txt), so this never affects image size or runtime.
-r requirements.txt
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<0.25
