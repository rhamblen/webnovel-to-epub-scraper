# Local development overrides (the container sets these via docker-compose instead).
# Copy to .env and point at writable local folders when running outside Docker.
WN_DATA_DIR=./.localdata/config
WN_OUTPUT_DIR=./.localdata/output
